# ------------------------------------------------------------
# Servidor
# ------------------------------------------------------------
NODE_ENV=development
PORT=3000

# ------------------------------------------------------------
# Seguridad
# ------------------------------------------------------------
# Cambia este valor en producción (cadena larga y aleatoria)
JWT_SECRET=cambia-esta-clave-en-produccion
JWT_EXPIRES_IN=8h

# ------------------------------------------------------------
# Almacenamiento
#   json  -> archivos JSON en backend/data (por defecto)
#   mysql -> MySQL/MariaDB vía Knex
# ------------------------------------------------------------
STORAGE_DRIVER=json

# ------------------------------------------------------------
# Rutas (opcionales, por defecto relativas a backend/)
# ------------------------------------------------------------
# DATA_DIR=./data
# UPLOAD_DIR=./uploads

# ------------------------------------------------------------
# MySQL / MariaDB (solo si STORAGE_DRIVER=mysql)
# ------------------------------------------------------------
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=incidencias
DB_PASSWORD=
DB_NAME=incidencias
DB_CHARSET=utf8mb4

# ------------------------------------------------------------
# Límites de evidencia (bytes / segundos)
# ------------------------------------------------------------
MAX_FOTO_BYTES=104857600
MAX_VIDEO_BYTES=1073741824
MAX_VIDEO_SEG=300
